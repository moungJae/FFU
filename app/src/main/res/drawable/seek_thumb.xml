<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false"  >
    <!-- 배경 -->
    <solid
        android:color="@color/white"/>
    <!-- 테두리 -->
    <stroke
        android:width="2.5dp"
        android:color="@color/weakBlue" />
    <!-- 크기 -->
    <size
        android:width="30dp"
        android:height="30dp"/>
</shape>